---
phase: 01-foundation-and-safety
plan: 04
type: execute
depends_on: ["01-03"]
files_modified:
  - .skills/claude-history-ingest/SKILL.md
  - .skills/codex-history-ingest/SKILL.md
  - .skills/wiki-lint/SKILL.md
  - .skills/wiki-rebuild/SKILL.md
  - .skills/cross-linker/SKILL.md
  - .skills/tag-taxonomy/SKILL.md
---

<objective>
Extend audit logging to the remaining write-path skills: history ingest skills (claude-history-ingest, codex-history-ingest) and maintenance skills (wiki-lint, wiki-rebuild, cross-linker, tag-taxonomy). After this plan, every operation that modifies the vault leaves an audit trail — completing PR #12.

Purpose: PR #12 Part B — Full audit coverage. Without this, maintenance operations (lint fixes, link additions, tag normalizations, rebuilds) are invisible in the audit log, creating gaps in the change history that PR #14 (mesh sync) would need to reason around.
Output: Six SKILL.md files updated with audit append steps. All write-path skills are now audited. Phase 1 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-safety/01-03-SUMMARY.md
@.skills/claude-history-ingest/SKILL.md
@.skills/codex-history-ingest/SKILL.md
@.skills/wiki-lint/SKILL.md
@.skills/wiki-rebuild/SKILL.md
@.skills/cross-linker/SKILL.md
@.skills/tag-taxonomy/SKILL.md

**Audit schema (from 01-03):**
Schema defined in llm-wiki/SKILL.md. All entries go to `$OBSIDIAN_VAULT_PATH/_meta/audit.jsonl`.

**Op values for maintenance skills:**
- claude-history-ingest / codex-history-ingest: `op: "ingest"`, `skill: "claude-history-ingest"` / `"codex-history-ingest"`, `action: "create"` or `"update"`
- wiki-lint auto-fix: `op: "lint-fix"`, `skill: "wiki-lint"`, `action: "fix"`, `page: <page fixed>`, `source: null`
- wiki-rebuild: `op: "rebuild"`, `skill: "wiki-rebuild"`, `action: "create"` per page written, `source: null` (rebuild from vault contents, no external source)
- cross-linker: `op: "link"`, `skill: "cross-linker"`, `action: "link"`, `page: <page that got links added>`, `source: null`
- tag-taxonomy: `op: "tag"`, `skill: "tag-taxonomy"`, `action: "tag"`, `page: <page with normalized tags>`, `source: null`

**Append pattern:** Same as 01-03 — one entry per page modified, appended AFTER the write.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add audit logging to claude-history-ingest and codex-history-ingest</name>
  <files>.skills/claude-history-ingest/SKILL.md, .skills/codex-history-ingest/SKILL.md</files>
  <action>
Both skills mine conversation history and write pages to the vault. The PII filter was already added in 01-02. Now add audit logging.

**For each skill**, find the step where a wiki page is written (via Write tool or equivalent instruction). After that write step, add:

```
**After each page write:** Append one entry to `$OBSIDIAN_VAULT_PATH/_meta/audit.jsonl`:
- `op`: "ingest"
- `skill`: "[skill name]"  (claude-history-ingest or codex-history-ingest)
- `page`: vault-relative path of page written
- `source`: conversation/session identifier (session ID, file path, or range of sessions processed)
- `action`: "create" if new page, "update" if existing page modified
```

If either skill writes multiple pages in a loop, the audit append is inside the loop.

Add checklist item to each: `[ ] Audit entries written to _meta/audit.jsonl for all pages created/updated`
  </action>
  <verify>grep -n "audit.jsonl" .skills/claude-history-ingest/SKILL.md .skills/codex-history-ingest/SKILL.md</verify>
  <done>Both history ingest skills append audit entries per page after each write. Consistent with wiki-ingest pattern from 01-03.</done>
</task>

<task type="auto">
  <name>Task 2: Add audit logging to wiki-lint, wiki-rebuild, cross-linker, tag-taxonomy</name>
  <files>.skills/wiki-lint/SKILL.md, .skills/wiki-rebuild/SKILL.md, .skills/cross-linker/SKILL.md, .skills/tag-taxonomy/SKILL.md</files>
  <action>
These are maintenance skills that modify existing vault pages. Each gets a focused audit append after their write steps.

**wiki-lint/SKILL.md:**
Find where wiki-lint auto-fixes a page (broken link repair, orphan linking, frontmatter corrections). After the fix write, append:
- `op: "lint-fix"`, `skill: "wiki-lint"`, `page: <fixed page>`, `source: null`, `action: "fix"`
Only append when a page is ACTUALLY MODIFIED (not on inspection-only runs). If wiki-lint runs in report-only mode (no auto-fix), no audit entry.
Add checklist: `[ ] Audit entries written for each page auto-fixed`

**wiki-rebuild/SKILL.md:**
Find where wiki-rebuild writes pages during a full vault rebuild. After each page write:
- `op: "rebuild"`, `skill: "wiki-rebuild"`, `page: <page written>`, `source: null`, `action: "create"`
For rebuild, all pages are "create" since rebuild starts from scratch.
Add checklist item.

**cross-linker/SKILL.md:**
Find where cross-linker adds wikilinks to a page. After modifying the page:
- `op: "link"`, `skill: "cross-linker"`, `page: <page modified>`, `source: null`, `action: "link"`
One entry per page that receives new links.
Add checklist item.

**tag-taxonomy/SKILL.md:**
Find where tag-taxonomy normalizes tags on a page. After modifying the frontmatter:
- `op: "tag"`, `skill: "tag-taxonomy"`, `page: <page modified>`, `source: null`, `action: "tag"`
One entry per page with normalized tags.
Add checklist item.
  </action>
  <verify>grep -l "audit.jsonl" .skills/wiki-lint/SKILL.md .skills/wiki-rebuild/SKILL.md .skills/cross-linker/SKILL.md .skills/tag-taxonomy/SKILL.md — all 4 should match.</verify>
  <done>All four maintenance skills append audit entries when they modify vault pages. Audit log now covers all write-path operations. PR #12 complete.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `grep -rl "audit.jsonl" .skills/` returns all 9 write-path skills: wiki-ingest, wiki-update, data-ingest, claude-history-ingest, codex-history-ingest, wiki-lint, wiki-rebuild, cross-linker, tag-taxonomy
- [ ] wiki-query and wiki-status are NOT in the list (they read, don't write)
- [ ] wiki-lint only appends when a page is auto-modified (not on report-only runs)
- [ ] All op values match the schema: ingest | update | lint-fix | rebuild | link | tag
- [ ] `bash setup.sh` passes (symlinks intact after SKILL.md edits) — run from `/home/jrm22n/projects/kwiki/`
</verification>

<success_criteria>
- All tasks completed
- 6 maintenance/history skills have audit append steps
- Total write-path coverage: all 9 skills that write to the vault
- Op values are consistent across all skills (no typos, no missing fields)
- setup.sh passes (symlinks are path-based, SKILL.md edits don't break them)
- Phase 1 complete: confidence frontmatter ✓, PII filter ✓, audit log ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-safety/01-04-SUMMARY.md`:

---
phase: 01-foundation-and-safety
plan: 04
subsystem: audit-log
provides: [audit-full-coverage]
affects: [06-02]
tech-stack:
  added: []
  patterns: [append-only-jsonl, per-page-audit-entry, conditional-audit-on-modify]
key-files:
  - .skills/claude-history-ingest/SKILL.md
  - .skills/codex-history-ingest/SKILL.md
  - .skills/wiki-lint/SKILL.md
  - .skills/wiki-rebuild/SKILL.md
  - .skills/cross-linker/SKILL.md
  - .skills/tag-taxonomy/SKILL.md
key-decisions:
  - "wiki-lint only audits when auto-fixing, not on report-only runs"
  - "wiki-rebuild uses action: create for all pages (rebuild = fresh write)"
---

# Phase 1 Plan 04: Audit Log (Maintenance Skills) Summary

**Extended audit logging to 6 remaining write-path skills, completing PR #12. All 9 write-path skills now emit audit entries to _meta/audit.jsonl.**

## Accomplishments

- Audit logging added to claude-history-ingest, codex-history-ingest, wiki-lint, wiki-rebuild, cross-linker, tag-taxonomy
- Full write-path coverage: 9/9 write-path skills audited
- Conditional audit in wiki-lint (only on auto-fix, not report-only)
- Phase 1 complete: confidence frontmatter + PII filter + audit log all shipped

## Files Created/Modified

- `.skills/claude-history-ingest/SKILL.md` — audit append step after page write
- `.skills/codex-history-ingest/SKILL.md` — audit append step after page write
- `.skills/wiki-lint/SKILL.md` — audit append on auto-fix (conditional)
- `.skills/wiki-rebuild/SKILL.md` — audit append per page during rebuild
- `.skills/cross-linker/SKILL.md` — audit append per page with new links
- `.skills/tag-taxonomy/SKILL.md` — audit append per page with normalized tags

## Decisions Made

- wiki-lint audit is conditional on actual modification — report-only runs don't pollute the log
- All maintenance ops use null source — they operate on existing vault content, not external sources

## Issues Encountered

[None or describe any]

## Next Step

Phase 1 complete. Ready for Phase 2: Quality + Graph Foundation (02-01-PLAN.md)
</output>
