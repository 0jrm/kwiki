---
phase: 05-cross-cutting-intelligence
plan: 01
type: execute
depends_on: ["01-01", "03-01", "02-01"]
files_modified:
  - .skills/wiki-ingest/SKILL.md
  - .skills/llm-wiki/SKILL.md
  - README.md
---

<objective>
PR #9: Add contradiction detection on write as an explicit preflight so ingest resolves conflicts intentionally instead of via ad hoc page-local edits.

Purpose: Use lifecycle + graph primitives already shipped to detect conflicting claims before writes, classify certainty, and apply deterministic resolution rules (supersede vs ambiguity retention).
Output: `wiki-ingest` includes a cross-page contradiction preflight; canonical docs and README describe the behavior and user-facing intent.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-lifecycle-layer/03-01-SUMMARY.md
@.planning/phases/04-graph-traversal-and-hybrid-search/04-01-SUMMARY.md
@.skills/wiki-ingest/SKILL.md
@.skills/llm-wiki/SKILL.md
@README.md

**Backward compat constraint (NON-NEGOTIABLE):** Existing vault pages without graph metadata or supersession fields must still ingest successfully. Contradiction preflight is additive and only affects pages being touched.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add contradiction preflight to wiki-ingest</name>
  <files>.skills/wiki-ingest/SKILL.md</files>
  <action>
Add a dedicated step before write/update that:
- builds candidate comparison pages from planned writes + graph/entity overlap
- compares incoming high-value claims against existing entity+attribute claims
- classifies outcomes (`no_conflict`, `possible_conflict`, `conflict`)
- records an explicit resolution strategy before applying write operations

Resolution semantics:
- `possible_conflict`: keep both claims, mark `^[ambiguous]`, add review note
- `conflict`: apply supersession flow

Do not permit silent overwrite of conflicting claims.
  </action>
  <verify>
- `grep -n "Contradiction Preflight\\|possible_conflict\\|no_conflict\\|Do not silently overwrite" .skills/wiki-ingest/SKILL.md`
  </verify>
  <done>wiki-ingest includes deterministic contradiction handling before writes.</done>
</task>

<task type="auto">
  <name>Task 2: Align llm-wiki canonical lifecycle guidance</name>
  <files>.skills/llm-wiki/SKILL.md</files>
  <action>
Document that write paths (ingest/crystallize) run contradiction checks and must choose supersession or ambiguity retention explicitly.
  </action>
  <verify>
- `grep -n "contradiction handling\\|ingest/crystallization\\|\\^\\[ambiguous\\]" .skills/llm-wiki/SKILL.md`
  </verify>
  <done>canonical docs reflect cross-cutting contradiction semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Update README with PR #9 capability</name>
  <files>README.md</files>
  <action>
Add user-facing note for write-time contradiction detection and expected behavior.
  </action>
  <verify>
- `grep -n "contradiction detection\\|supersedes\\|ambiguous" README.md`
  </verify>
  <done>README exposes contradiction-aware write behavior as a first-class feature.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `wiki-ingest` contains contradiction preflight and explicit resolution classes
- [ ] Silent conflicting overwrite is explicitly disallowed
- [ ] llm-wiki + README align with write-time contradiction behavior
</verification>

<success_criteria>
- All tasks complete
- Contradiction handling is explicit before writes, not implicit after the fact
- Backward compatibility preserved for legacy pages
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-cutting-intelligence/05-01-SUMMARY.md` with frontmatter:
- subsystem: `write-time-contradiction-detection`
- provides: `[cross-page-conflict-preflight, deterministic-resolution-strategy, ambiguity-preservation]`
- requires: `[confidence-fields, supersession-chain, typed-graph-jsonl]`
- affects: `[05-02, 06-02]`
</output>
